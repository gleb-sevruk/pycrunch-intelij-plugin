<idea-plugin>
    <id>com.pycrunch.my_gradle_plugin</id>
    <name>PyCrunch Engine Connector</name>
    <vendor email="sevruk.gleb@gmail.com" url="http://www.nothing.here">PyCrunch</vendor>

    <description><![CDATA[
    Enter 2 short description for your plugin here.<br>
    <em>most  s HTML tags may be used 2</em>
    ]]></description>

    <!-- please see http://www.jetbrains.org/intellij/sdk/docs/basics/getting_started/plugin_compatibility.html
         on how to target different products -->
<!--    &lt;!&ndash; uncomment to enable plugin in all products-->
    <depends>com.intellij.modules.lang</depends>
<!--    &ndash;&gt;-->
    <project-components>
        <component>
            <!-- Interface and implementation classes are the same -->
            <implementation-class>MyProjComponent</implementation-class>

            <!-- If the "workspace" option is set "true", the component
                 saves its state to the .iws file instead of the .ipr file.
                 Note that the <option> element is used only if the component
                 implements the JDOMExternalizable interface. Otherwise, the
                 use of the <option> element takes no effect.  -->
            <option name="workspace" value="true" />

            <!-- If the "loadForDefaultProject" tag is present, the project component is instantiated also for the default project. -->
            <loadForDefaultProject/>
        </component>
    </project-components>
    <extensions defaultExtensionNs="com.intellij">
        <applicationService serviceInterface="MyCounter"
                            serviceImplementation="MyCounter"/>
        <applicationService serviceInterface="MyPycrunchConnector"
                            serviceImplementation="MyPycrunchConnector"/>
        <toolWindow id="PyCrunch" icon="/cherry-icon.png" anchor="right" factoryClass="MyToolWindowFactory"/>


    </extensions>

    <actions>
        <group id="PyChrunch.DevMenu" text="_PyCrunch" description="PyCrunch">
            <add-to-group group-id="MainMenu" anchor="last"  />
            <action id="PyChrunch.Textboxes" class="TextBoxes" text="Attach to PyCrunch-Engine" description="A test menu item" />
            <action id="PyCrunch.DiscoveryDisplay" class="PycrunchDiscoveryDisplay" text="Show _Tests" description="A test menu item" />
        </group>
    </actions>
</idea-plugin>